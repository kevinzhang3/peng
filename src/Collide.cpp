#include <Body.h>
#include <Util.h>

int DetectCollision(Body& body) {
    // if collision
    return 0;
}

int CollideBody(Body& bodyA, Body& bodyB) {
    // call after detect
    return 0;
}

int CollideBarrier(Body& body) {
    
    return 0;
}
